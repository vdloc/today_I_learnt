# API

1. render mothod :

   ```javascript
   Mustache.render(
   template: String,
     view : JSON object,
     partials? : Object,
     tags?
   ) => String
   Mustache.parse(
   	template: String,
     tags?
   )
   ```

   