1. Sometimes, when we change somethings in the repository that we’re working on, but it doesn’t work as we looking for, we should use:

   ```bash
   git rm --cached . "to remove all cached files "
   ```

   